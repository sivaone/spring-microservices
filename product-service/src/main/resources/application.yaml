server:
  port: 8083
  tomcat:
    mbeanregistry:
      enabled: true
spring:
  application:
    name: product
  config:
    import: configserver:http://localhost:8888
  jpa:
    hibernate:
      ddl-auto: validate
  zipkin:
    baseUrl: http://localhost:9411/
management:
  endpoints:
    web:
      exposure:
        include: "*"