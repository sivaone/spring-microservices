server:
  port: 8085
spring:
  application:
    name: order
  config:
    import: configserver:http://localhost:8888
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://<domain>.auth0.com/
